# Parallel grep. From http://www.gnu.org/software/parallel/man.html#example__parallel_grep
[[ -z "$1" ]] && echo 'Please enter an expression to match' && return 1
find . -type f | parallel -k -j150% -n 1000 -m grep -H -n "$1" {}